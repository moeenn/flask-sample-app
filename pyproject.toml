[project]
name = "flask-sample-app"
version = "1.0.0"
dependencies = [
    "Flask",
    "dotenv-cli",
    "Flask-SQLAlchemy",
    "Flask-Migrate",
    "invoke,
    "gunicorn",
    "uvicorn",
    "turbo-flask",
    "ruff",
    "black",
]

[tool.black]
line-length = 80